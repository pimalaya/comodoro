---
cairn: tasks
change: first-run-wizard
---

# Tasks

- [x] Add the `configure` command, asking for a cycle preset and the endpoints to serve over
- [x] Name the account after the preset, suffixed until the configuration does not hold it
- [x] Write a missing file whole, append a plain text block to an existing one, print when neither
- [x] Claim `default` only when no other account does
- [x] Raise the offer from a bare invocation and from a command needing an account, then carry on
- [x] Skip every prompt when stdin is not a terminal or `--json` is set
- [x] Name the path, the accounts and the ways to pick a default in the three resolution failures
- [x] Honour `COMODORO_CONFIG` and `:`-delimited `-c` paths, both documented but missing
- [x] Cover the rendering, the append and the name suffixing with tests
- [x] Update README.md, config.sample.toml and CHANGELOG.md
